name: flutter_pagination
description: A simple page number indicator for flutter.
version: 0.1.1
homepage: https://github.com/3bdelrahmann/flutter_pagination

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  scroll_to_id: ^1.5.1
  scrollable_positioned_list: ^0.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3


flutter:
